"""
GDPR Healthcare Pipeline - Test Suite

Run all tests: pytest
Run by phase: pytest -m phase1, pytest -m phase2, pytest -m phase3
Run fast tests only: pytest -m "not slow"
Run integration tests: pytest -m integration
"""
